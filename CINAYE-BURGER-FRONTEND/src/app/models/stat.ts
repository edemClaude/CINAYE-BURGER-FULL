export class Stat {

   customer_count?: number; // count of customers
   burger_count?: number; // count of burgers
   order_count?: number; //  count of order
   payment_count?: number; // count of payment
   total_revenue?: number; // total revenue
   daily_order_count?: number; // daily count of order
   daily_validated_order_count?: number; // daily count of validated orders
   daily_pending_order_count?: number; // daily count of pending orders
   daily_cancelled_order_count?: number; // daily count of cancelled orders
   daily_revenue?: number; // daily revenue

}
